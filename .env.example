# Environment Variables for Website Growth Analyzer
# Copy this file to .env for local development
# For production, set these in Railway dashboard under "Variables" tab

# === REQUIRED API KEYS ===
# TODO: Get your Firecrawl API key from https://firecrawl.dev
FIRECRAWL_API_KEY=fc-your-firecrawl-api-key-here

# TODO: Get your Anthropic API key from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# === SERVER CONFIGURATION ===
# Port for the server (Railway will set this automatically)
PORT=3000

# Environment type
NODE_ENV=development

# Frontend URL for CORS (not needed for full-stack deployment)
CLIENT_URL=http://localhost:5173

# === SETUP INSTRUCTIONS ===
# 1. Sign up for Firecrawl at https://firecrawl.dev
#    - Navigate to API Keys section
#    - Create a new API key
#    - Copy and paste it above (starts with "fc-")

# 2. Sign up for Anthropic at https://console.anthropic.com
#    - Go to API Keys section  
#    - Create a new API key
#    - Copy and paste it above (starts with "sk-ant-")

# 3. For Railway deployment:
#    - Do NOT commit this file to GitHub
#    - Add these variables in Railway dashboard:
#      Project Settings > Variables tab
#    - Railway will automatically set PORT

# === SECURITY NOTES ===
# - Never commit .env file to GitHub
# - Keep API keys secure and rotate them periodically
# - Use different API keys for development and production
# - Monitor API usage to prevent unexpected charges